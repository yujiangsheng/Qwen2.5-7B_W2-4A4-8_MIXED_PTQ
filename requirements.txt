# Qwen2.5-7B 混合精度 PTQ (W4 + A4/A8) 依赖包
# ===========================================

# 核心依赖
torch>=2.0.0
transformers>=4.35.0
numpy>=1.24.0
tqdm>=4.65.0

# 模拟量化 (mixed_precision_ptq.py)
datasets>=2.14.0
accelerate>=0.24.0

# 真实量化 (compare_real_quant.py)
huggingface-hub>=0.20.0
gguf>=0.6.0

# llama-cpp-python 需单独安装:
# macOS (Metal): CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python
# Linux (CUDA):  CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python
